mysql> select m.ename mgr from emp e, emp m where e.mgr=m.empno group by mgr having count(*)=(select max(R1)
    ->  from(select m.ename mgr, count(*) R1 from emp e join emp m on e.mgr=m.empno group by mgr)T1);
